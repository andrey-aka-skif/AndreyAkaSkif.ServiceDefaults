{
    // Часть блока "PostgreSQL" из Program.cs. Имя строки подключения
    // (DefaultConnection) фиксировано в пакете AndreyAkaSkif.ServiceDefaults.PostgreSQL
    // "ConnectionStrings": {
    //     "DefaultConnection": "Host=localhost;Port=5432;Database=samples;Username=postgres;Password=postgres"
    // },
    "Serilog": {
        "MinimumLevel": {
            "Default": "Debug",
            "Override": {
                "Microsoft.AspNetCore": "Information"
            }
        }
    }
}
